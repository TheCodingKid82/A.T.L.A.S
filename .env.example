# Database (Railway provides this automatically via Postgres plugin)
DATABASE_URL="postgresql://atlas:atlas_dev_password@localhost:5432/atlas?schema=public"

# Supermemory
SUPERMEMORY_API_KEY="your-supermemory-api-key"
SUPERMEMORY_BASE_URL="https://api.supermemory.com"

# Dashboard — URL of the MCP server (set to Railway's MCP service URL in production)
NEXT_PUBLIC_MCP_SERVER_URL="http://localhost:3001"

# Encryption (generate with: openssl rand -hex 32)
ENCRYPTION_KEY="generate-a-64-char-hex-string-for-aes-256-encryption-key-here!!"

# Worker
WORKER_API_KEY="atl_worker_..."
ATLAS_MCP_URL="http://localhost:3001/mcp"
DEFAULT_WORKING_DIR="/tmp"

# Worker — CLI & MCP auth (required for C.O.D.E. to commit/deploy/automate)
GH_TOKEN="ghp_..."
RAILWAY_API_TOKEN="your-railway-api-token"
ZAPIER_MCP_TOKEN="your-zapier-mcp-token"
